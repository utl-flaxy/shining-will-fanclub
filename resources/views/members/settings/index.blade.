@extends('members.layouts.app')

@section('title', '設定')

@section('content')

<style>
.settings-wrapper {
    max-width: 480px;
    margin: 0 auto;
    padding: 16px;
}

.page-title {
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 20px;
}

.section-label {
    font-size: 13px;
    color: #666;
    margin-bottom: 8px;
}

.settings-item {
    display: flex;
    gap: 12px;
    padding: 14px 16px;
    background: #fff;
    border-radius: 14px;
    margin-bottom: 10px;
    text-decoration: none;
    color: #111;
    box-shadow: 0 2px 8px rgba(0,0,0,0.05);
}

.item-icon {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    background: #f1f1f5;
    display: flex;
    align-items: center;
    justify-content: center;
}
</style>

<div class="settings-wrapper">

    <div class="page-title">設定</div>

    <div class="section-label">アカウント</div>
    <a href="{{ route('members.settings.account') }}" class="settings-item">
        <div class="item-icon">👤</div>
        <div>
            <div>アカウント情報</div>
            <small>メールアドレス・パスワード</small>
        </div>
    </a>

    <div class="section-label">購入・アイテム</div>
    <a href="{{ route('members.settings.purchases') }}" class="settings-item">
        <div class="item-icon">🧾</div>
        <div>購入履歴</div>
    </a>

    <a href="{{ route('members.settings.membership') }}" class="settings-item">
        <div class="item-icon">💳</div>
        <div>デジタル会員証</div>
    </a>

    <a href="{{ route('members.settings.themes') }}" class="settings-item">
        <div class="item-icon">🎨</div>
        <div>テーマ</div>
    </a>

    <div class="section-label">その他</div>
    <form method="POST" action="{{ route('logout') }}">
        @csrf
        <button class="settings-item" style="width:100%;border:none;">
            <div class="item-icon">🚪</div>
            <div>ログアウト</div>
        </button>
    </form>

</div>

@endsection
